import React from 'react';
import {Container} from "react-bootstrap";

const OrderInfo = () => {
    return (
        <Container>
            Order Info
        </Container>
    );
};

export default OrderInfo;